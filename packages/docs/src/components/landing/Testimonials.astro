<section class="quote not-content">
    <blockquote>"60% of the time, it works every time."</blockquote>
    <cite>- Brian Fantana, the OG of analytics</cite>
</section>

<style>
    .quote {
        padding-block: clamp(2rem, 5vw, 4rem);
        border-top: 1px solid var(--sl-color-gray-5);
        text-align: center;
    }

    .quote blockquote {
        font-size: var(--sl-text-2xl);
        font-weight: 600;
        color: var(--sl-color-white);
        margin: 0 0 0.75rem;
        padding: 2rem 0 0;
        line-height: 1.3;
    }

    .quote cite {
        font-size: var(--sl-text-sm);
        color: var(--sl-color-gray-2);
        font-style: normal;
    }
</style>
